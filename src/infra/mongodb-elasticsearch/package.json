{
  "name": "sync-mongodb-elasticsearch",
  "version": "1.0.0",
  "description": "Sync mongodb and elasticsearch via mongo reactive streams (mongo must be a replica set!)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.7.0",
    "dotenv": "^16.0.3",
    "mongodb": "^3.5.2"
  }
}
