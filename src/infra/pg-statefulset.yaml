apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pg-stateful-users
spec:
  serviceName: pg-service-users
  replicas: 1
  selector:
    matchLabels:
      app: postgres-users
  template:
    metadata:
      labels:
        app: postgres-users
    spec:
      containers:
        - name: user-service-postgres
          image: postgres:14.6-alpine
          imagePullPolicy: Always
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pg-secrets-users
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-secrets-users
                  key: POSTGRES_PASSWORD
      volumes:
        - name: user-service-db
          hostPath:
            path: /data
  volumeClaimTemplates:
    - metadata:
        name: user-service-db
      spec:
        accessModes:
          - "ReadWriteOnce"
        resources:
          requests:
            storage: 1Gi

